const BACKEND='https://script.google.com/macros/s/AKfycbxUPLh0vcc6bLqsBNsCI8KqrvUhOSllC1ros6hJbLFrgvE5UBZtQA-pKAI12y51qVH1/exec';
const MAX_QR=2048,FALLBACK_DELAY=2500,FALLBACK_INTERVAL=350,CAMERA={facingMode:'environment',width:{ideal:1920},height:{ideal:1080}},$=id=>document.getElementById(id);let scanner=null,redirecting=false,lastRejected='',fallbackTimer=null,fallbackLoopTimer=null,zxingReader=null,zxingPromise=null,cameraInfo={},metrics=null;
export function validateQr(value){const text=String(value||'').trim();if(!text)throw new Error('QR DECODIFICADO — CONTEÚDO NÃO SUPORTADO: conteúdo vazio.');if(text.length>MAX_QR)throw new Error('QR DECODIFICADO — CONTEÚDO NÃO SUPORTADO: conteúdo excessivo.');let url;try{url=new URL(text)}catch{throw new Error('QR DECODIFICADO — CONTEÚDO NÃO SUPORTADO: não é uma URL HTTPS.')}if(url.protocol!=='https:')throw new Error('QR DECODIFICADO — CONTEÚDO NÃO SUPORTADO: esquema não permitido.');return url.href}
export function backendUrl(value){return `${BACKEND}?qr=${encodeURIComponent(validateQr(value))}`}
export function qrDiagnostic(value){const raw=String(value??''),d={conteudoBruto:raw,comprimento:raw.length,protocolo:'',hostname:'',pathname:'',parametros:{}};try{const u=new URL(raw.trim());d.protocolo=u.protocol;d.hostname=u.hostname;d.pathname=u.pathname;for(const [key,val] of u.searchParams){if(Object.keys(d.parametros).length>=8)break;d.parametros[key]=val.slice(0,300)}}catch{}return d}
export function buildOptionalConstraints(capabilities={}){const constraints={};if(capabilities.width)constraints.width={ideal:Math.min(1920,capabilities.width.max||1920)};if(capabilities.height)constraints.height={ideal:Math.min(1080,capabilities.height.max||1080)};const focus=Array.isArray(capabilities.focusMode)?capabilities.focusMode:[];if(focus.includes('continuous'))constraints.advanced=[{focusMode:'continuous'}];return constraints}
export function fullScanRegion(video){const width=video.videoWidth||1280,height=video.videoHeight||720,downWidth=Math.min(1280,width),downHeight=Math.max(1,Math.round(height*downWidth/width));return{x:0,y:0,width,height,downScaledWidth:downWidth,downScaledHeight:downHeight}}
export function frameSize(width,height,maxWidth){const sourceWidth=width||1280,sourceHeight=height||720,w=Math.min(maxWidth,sourceWidth),h=Math.max(1,Math.round(sourceHeight*w/sourceWidth));return{width:w,height:h}}
export function newMetrics(now=0){return{inicio:now,primeiraTentativaMs:null,framesPrimario:0,framesFallback:0,decoderAtivo:'qr-scanner',fallbackAcionado:false,timeoutFallbackMs:FALLBACK_DELAY,resultado:'A — QR NÃO DETECTADO'}}
export function recordAttempt(state,decoder,now){if(state.primeiraTentativaMs===null)state.primeiraTentativaMs=Math.max(0,Math.round(now-state.inicio));if(decoder==='ZXing')state.framesFallback+=1;else state.framesPrimario+=1;const elapsed=Math.max(1,now-state.inicio);state.fpsEfetivo=Number(((state.framesPrimario+state.framesFallback)*1000/elapsed).toFixed(1));return state}
function message(text,error=false){$('message').hidden=false;$('message').className=`card ${error?'error':'ok'}`;$('message').textContent=text}
function cameraError(error){const text=`${error?.name||''} ${error?.message||error||''}`.toLowerCase();if(/notallowed|permission|permiss/.test(text))return'ERRO DE CÂMERA: permissão negada. Libere a câmera nos ajustes e tente novamente.';if(/notfound|overconstrained|device/.test(text))return'ERRO DE CÂMERA: câmera indisponível neste dispositivo.';return'ERRO DE CÂMERA: não foi possível abrir a câmera. Tente novamente ou use Colar link.'}
function showRejected(value,error){lastRejected=String(value??'');$('qrDiag').hidden=false;$('qrDiagText').textContent=JSON.stringify(qrDiagnostic(lastRejected),null,2);message(error.message,true)}
export function redirectQr(value){location.assign(backendUrl(value))}
function now(){return typeof performance!=='undefined'?performance.now():Date.now()}
function updateDiagnostic(){if(!$('cameraDiag')||!metrics)return;const video=$('video'),primary=fullScanRegion(video),fallback=frameSize(video.videoWidth||1920,video.videoHeight||1080,1920);$('cameraDiag').textContent=JSON.stringify({...cameraInfo,decodificacao:{...metrics,resolucaoVideo:`${video.videoWidth||0}x${video.videoHeight||0}`,framePrimario:`${primary.downScaledWidth}x${primary.downScaledHeight}`,frameFallback:`${fallback.width}x${fallback.height}`,decoderPrimario:'qr-scanner 1.4.2',decoderFallback:'ZXing Browser 0.2.1',deteccaoSemDecodificacao:'não exposta pelos decoders; frames/tentativas são a métrica equivalente'}},null,2)}
function attempted(decoder){if(metrics)recordAttempt(metrics,decoder,now());updateDiagnostic()}
function clearFallback(){clearTimeout(fallbackTimer);clearTimeout(fallbackLoopTimer);fallbackTimer=null;fallbackLoopTimer=null}
async function stop(){clearFallback();if(scanner){scanner.stop();scanner.destroy();scanner=null}const stream=$('video').srcObject;if(stream)stream.getTracks().forEach(track=>track.stop());$('video').srcObject=null;$('camera').hidden=true}
async function tuneCamera(){const track=$('video').srcObject?.getVideoTracks?.()[0];if(!track)return{};const capabilities=track.getCapabilities?track.getCapabilities():{},before=track.getSettings?track.getSettings():{},constraints=buildOptionalConstraints(capabilities);if(track.applyConstraints&&Object.keys(constraints).length)try{await track.applyConstraints(constraints)}catch(error){message(`Câmera ativa; ajuste opcional não suportado (${error?.name||'erro'}).`)}const settings=track.getSettings?track.getSettings():before;cameraInfo={settings,capabilities:{width:capabilities.width||null,height:capabilities.height||null,facingMode:capabilities.facingMode||null,focusMode:capabilities.focusMode||null,zoom:capabilities.zoom||null},aplicado:{resolucao:Boolean(constraints.width||constraints.height),focusContinuo:Boolean(constraints.advanced),zoom:false}};updateDiagnostic();return cameraInfo}
function loadZxing(){if(zxingPromise)return zxingPromise;zxingPromise=new Promise((resolve,reject)=>{if(globalThis.ZXingBrowser)return resolve(globalThis.ZXingBrowser);const script=document.createElement('script');script.src='./vendor/zxing-browser-0.2.1.min.js';script.onload=()=>resolve(globalThis.ZXingBrowser);script.onerror=()=>reject(new Error('bundle ZXing indisponível'));document.head.appendChild(script)});return zxingPromise}
function canvasFrame(maxWidth){const video=$('video'),size=frameSize(video.videoWidth,video.videoHeight,maxWidth),canvas=$('frame');canvas.width=size.width;canvas.height=size.height;canvas.getContext('2d',{willReadFrequently:true}).drawImage(video,0,0,size.width,size.height);return canvas}
async function decodeFallback(maxWidth=1920){const ZXing=await loadZxing();zxingReader ||= new ZXing.BrowserQRCodeReader();attempted('ZXing');const result=zxingReader.decodeFromCanvas(canvasFrame(maxWidth));return result.getText?result.getText():String(result)}
async function fallbackTick(){if(redirecting||!scanner)return;try{const value=await decodeFallback();metrics.resultado='C — QR DECODIFICADO';metrics.decoderAtivo='ZXing fallback';updateDiagnostic();await decoded(value,'ZXing fallback')}catch{}if(!redirecting&&scanner)fallbackLoopTimer=setTimeout(fallbackTick,FALLBACK_INTERVAL)}
async function activateFallback(){if(redirecting||!scanner)return;metrics.fallbackAcionado=true;metrics.decoderAtivo='qr-scanner + ZXing fallback';metrics.resultado='A — QR NÃO DETECTADO; fallback acionado';updateDiagnostic();$('capture').hidden=false;message("QR ainda não identificado. Mantenha-o inteiro no quadro ou tente 'Capturar imagem'.");await fallbackTick()}
async function captureImage(){if(redirecting||!scanner)return;message('Tentando leitura local da imagem capturada…');try{const value=await decodeFallback(2560);metrics.resultado='C — QR DECODIFICADO';metrics.decoderAtivo='ZXing captura manual';updateDiagnostic();await decoded(value,'ZXing captura manual')}catch{metrics.resultado='B — imagem capturada, mas não decodificada';updateDiagnostic();message("QR não decodificado na imagem. Afaste ou aproxime um pouco e tente 'Capturar imagem' novamente.",true)}}
async function decoded(value,decoder='qr-scanner'){if(redirecting)return;try{const target=backendUrl(value);redirecting=true;if(metrics){metrics.resultado='C — QR DECODIFICADO';metrics.decoderAtivo=decoder;updateDiagnostic()}message(`QR DECODIFICADO — URL FISCAL ENVIADA AO BACKEND (${decoder})`);await stop();location.assign(target)}catch(error){if(metrics){metrics.resultado='QR DECODIFICADO — CONTEÚDO NÃO SUPORTADO';metrics.decoderAtivo=decoder;updateDiagnostic()}showRejected(value,error)}}
async function start(){await stop();redirecting=false;metrics=newMetrics(now());cameraInfo={};$('camera').hidden=false;$('capture').hidden=true;$('qrDiag').hidden=true;message('Abrindo câmera…');try{const {default:QrScanner}=await import('https://cdn.jsdelivr.net/npm/qr-scanner@1.4.2/qr-scanner.min.js');scanner=new QrScanner($('video'),result=>{attempted('qr-scanner');decoded(result.data||result,'qr-scanner')},{preferredCamera:CAMERA.facingMode,calculateScanRegion:fullScanRegion,maxScansPerSecond:25,returnDetailedScanResult:true,onDecodeError:error=>{attempted('qr-scanner');if(error!==QrScanner.NO_QR_CODE_FOUND)message(`ERRO DO DECODER: ${error?.message||error}`,true)}});await scanner.start();await tuneCamera();message('Câmera ativa. Mantenha o QR inteiro dentro do quadro e aproxime ou afaste até ficar nítido.');fallbackTimer=setTimeout(activateFallback,FALLBACK_DELAY)}catch(error){await stop();message(cameraError(error),true)}}
async function copyRejected(){if(!lastRejected)return;try{await navigator.clipboard.writeText(lastRejected);message('Conteúdo QR copiado.')}catch{message('Não foi possível copiar automaticamente. Selecione o conteúdo exibido.',true)}}
if(typeof document!=='undefined'){$('scan').onclick=start;$('cancel').onclick=()=>{stop();message('Leitura cancelada. Você pode tentar novamente.')};$('capture').onclick=captureImage;$('paste').onclick=()=>{$('manual').hidden=!$('manual').hidden};$('send').onclick=()=>decoded($('content').value,'link colado');$('copyQr').onclick=copyRejected}
